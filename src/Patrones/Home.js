import React from 'react';
import { Link } from 'react-router-dom';


function Home() {
    //const navigate = useNavigate();

    const navegar = (rut) => {
        //navigate("/Singleton");
    }

    return (
        <div>
            <div >{'----------------'}</div>
        </div >
    )
}

export default Home;
